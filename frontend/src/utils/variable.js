export const BASE_URL = process.env.REACT_APP_BASE_URL;

export const PORT = process.env.PORT;
// the environment variables are injected at build time, not run time.
// changing any environment variables will require restarting the development server if it is running.